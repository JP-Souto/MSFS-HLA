#include <windows.h>
#include <iostream>
#include <SimConnect.h>
/*
SimConnect* simConnect = nullptr;
HRESULT hr;

int main()
{
    // Tente conectar ao Flight Simulator
    hr = SimConnect_Open(&simConnect, "MSFS-HLA", nullptr, 0, 0, 0);

    if (hr == S_OK) {
        std::cout << "Conexão com o Flight Simulator bem-sucedida!" << std::endl;
    }
    else {
        std::cout << "Falha na conexão com o Flight Simulator." << std::endl;
    }

    // Fechar a conexão com o Flight Simulator quando terminar
    if (simConnect) {
        SimConnect_Close(simConnect);
    }

    return 0;
}
*/